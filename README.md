This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Available Scripts

## Version react
v17.0.1

In the project directory, you can run:

### `yarn start`

Runs the app in the development mode.<br />
Open [http://localhost:7001](http://localhost:7001) to view it in the browser.

## Compilar y lanzar en producción
npm run build
pm2 serve build 7001 --spa


## data run por linea de comando s
$env:OPENSSL_CONF = $null

··CURRENT BACKGROUNT COLOR #f509ceff
